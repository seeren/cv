function SectionHeading({ title }: { title: string }) {
    return (
        <h2 className="text-lg font-semibold text-blue-dark uppercase tracking-wide ">
            {title}
        </h2>
    );
}

export default SectionHeading;